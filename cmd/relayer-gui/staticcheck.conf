# Every staticcheck rule applies to this module, ST1005 included.
#
# It was disabled here while the sentinel errors carried the sentence shown to
# the operator: they crossed the Wails bridge and were rendered verbatim, so
# they were capitalised and punctuated like the interface text they were.
#
# The error values are now ordinary Go errors, and the sentence an operator
# reads is produced where it is displayed — lib/safety.ts casts a native message
# into a sentence, which is the layer that owns presentation. That is the
# separation the exception was a placeholder for.
checks = ["all"]
